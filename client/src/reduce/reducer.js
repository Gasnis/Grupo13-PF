import {
    //importar aquí las actions
} from "./actions"

const initialState = {
    
}

export default function reducer (state = initialState, action) {
    switch(action.type){
        

        default:
            return state;
    }
}